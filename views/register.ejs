<!DOCTYPE html>
<html lang="pt-pt">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Registrar</title>
  <!-- Bootstrap CSS via CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #0d3b66;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      color: white;
    }
    .register-container {
      background: rgba(255, 255, 255, 0.1);
      padding: 2rem;
      border-radius: 12px;
      width: 360px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.3);
    }
    input.form-control {
      background: rgba(255, 255, 255, 0.2);
      border: none;
      color: white;
    }
    input.form-control::placeholder {
      color: #d1d1d1;
    }
    input.form-control:focus {
      background: rgba(255, 255, 255, 0.3);
      color: white;
      box-shadow: none;
    }
    button.btn-primary {
      width: 100%;
    }
    .login-link {
      margin-top: 1rem;
      display: block;
      color: #aad8ff;
      text-decoration: none;
      text-align: center;
    }
    .login-link:hover {
      text-decoration: underline;
      color: #fff;
    }
  </style>
</head>
<body>
  <div class="register-container text-center">
    <h2 class="mb-4">Registrar</h2>
    <form method="POST" action="/register">
      <div class="mb-3 text-start">
        <label for="fullname" class="form-label">Nome completo</label>
        <input id="fullname" name="fullname" class="form-control" placeholder="Nome completo" required autofocus />
      </div>
      <div class="mb-3 text-start">
        <label for="username" class="form-label">Usuário</label>
        <input id="username" name="username" class="form-control" placeholder="Usuário" required />
      </div>
      <div class="mb-3 text-start">
        <label for="phone" class="form-label">Número de Telemóvel</label>
        <input id="phone" name="phone" type="tel" class="form-control" placeholder="912345678" pattern="[0-9]{9}" required />
      </div>
      <div class="mb-3 text-start">
        <label for="password" class="form-label">Senha</label>
        <input id="password" name="password" type="password" class="form-control" placeholder="Senha" required />
      </div>
      <div class="mb-3 text-start">
        <label for="passwordConfirm" class="form-label">Confirmar Senha</label>
        <input id="passwordConfirm" name="passwordConfirm" type="password" class="form-control" placeholder="Confirmar senha" required />
      </div>
      <button class="btn btn-primary" type="submit">Cadastrar</button>
    </form>
    <a href="/login" class="login-link">Já tem conta? Faça login aqui</a>
  </div>
</body>
</html>